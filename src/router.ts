import Vue from 'vue';
import Router from 'vue-router';

Vue.use(Router);

export default new Router({
  routes: [
    // TODO - 6: Configure the routes:
    //   '/' - should lead redirect to the Users view
    //   '/users' - should open the Users view
    //   '/users/:userId/posts - should open the Posts view
  ],
});
